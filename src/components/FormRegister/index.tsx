'use client';

export const FormRegister = () => {
  return null;
  // const [state, action, isPending] = useActionState(createUser, InitialRegisterResponse);
  // return (
  //   <form action={action} autoComplete="on">
  //     <label>
  //       Name:
  //       <input name="name" type="name" />
  //       {state.erros.name?.map((error) => (
  //         <p key={error}>{error}</p>
  //       ))}
  //     </label>
  //     <label>
  //       Email:
  //       <input name="email" type="email" />
  //       {state.erros.email?.map((error) => (
  //         <p key={error}>{error}</p>
  //       ))}
  //     </label>
  //     <label>
  //       Password:
  //       <input name="password" type="password" />
  //       {state.erros.password?.map((error) => (
  //         <p key={error}>{error}</p>
  //       ))}
  //     </label>
  //     {state.message && <p>{state.message}</p>}
  //     <button type="submit">{isPending ? 'Carregando' : 'Criar conta'}</button>
  //   </form>
  // );
};
